import React from 'react';

const RequiredNotice = () => (
    <p className="required-note">Campos marcados com <span className="required">*</span> são obrigatórios.</p>
);

export default RequiredNotice;